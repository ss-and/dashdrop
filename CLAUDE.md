# DashDrop — 作業のときの決まり

Excelを置くとダッシュボードになる、日本の中小企業向けSaaS。
責任者は坂井。実装・検証の作法はここに集約する。

## 製品の芯

約束は1つ。**Excelを置いたらダッシュボードが出る**。
この体験を途中で止めるものは、機能として正しくても優先度が下がる。

相手は従業員20〜50人の会社のオーナーと事務。BIツールは触ったことがない。
**設定させない・学ばせない。**

競合は Tableau ではなく「ChatGPTにCSVを貼って聞く」。勝ち筋は
**決定的であること**（同じファイルなら毎回同じ答え）と**外にデータを出さないこと**。

## 4つの門（コミット前に必ず全部）

```bash
npx tsc --noEmit     # 型
npm run lint         # 作法
npm test             # テスト（2,700件以上・8秒前後）
npm run build        # 本番ビルド
```

1つでも赤ければコミットしない。
CI（`.github/workflows/ci.yml`）も同じ4つを回す。`gh run list` で実際に緑かを見ること
——**赤いまま放置されたCIは、無いCIより悪い**（誰も見なくなる）。
実際に10日間気づかれずに赤かった（`package-lock.json` のずれで `npm ci` が9秒で落ちていた）。

## 変異テスト — 省略しない

テストを書いたら、**実装をわざと壊して、テストが落ちることを確かめる**。
落ちなければそのテストは嘘。緑になるので書いた本人には効いて見えるが、
守られているという誤った安心を生むぶん無いより悪い。

```bash
cp src/lib/対象.ts /tmp/bak.ts
# 条件を1つ消す・しきい値を変える・優先順位を逆にする
npx vitest run tests/対象.test.ts   # 落ちることを確認
cp /tmp/bak.ts src/lib/対象.ts       # 必ず戻す
```

**生き残った変異は、テストのほうを書き直す**（実装を変えるのではない）。

実際に生き残った例:
- 正しい列がたまたま左にあったので、優先順位が壊れても並び順で正解に見えていた
- 重複が1〜2本では中央値が動かないので、畳む処理を外しても通っていた

## ブラウザ確認

テストを何千件書いても捕まらないものがある。実際に踏んだもの:
- バナーが**1文字ずつ縦に折り返し**、画面の3分の1を占領
- KPIの金額が `overflow-hidden` で**桁ごと切れて**「¥1,234」と読めた
- パネルの幅は収まっているのに**右半分が画面の外**

Playwright で**撮って目で見る**。iPhone 13（390px）で必ず1周する。

**開発サーバーを勝手に立てないこと。** 既に 3000 で動いていることが多く、
同じ `.next` を書き合うと**エラーも出ずにスタイルが全部外れる**（4回止まった）。
先に `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/login` で確かめる。

## コードの書き方

- コメントは**日本語**。「何をしているか」ではなく
  **「なぜそうしたか」「そうしなかったとき実際に何が起きたか」**を書く
- **できないことを約束しない。** 画面の文言と実装を一致させる
- **数を文言に直接書かない。** 実装から数える（`WIDGET_TYPES.length` のように）。
  手で書いた数字は増減のたびに必ずずれる（実際に2回ずれた）
- **打ち切りを黙って行わない。** 件数を切ったら、切ったと画面に出す
- **迷ったら「やらない」側へ。** 間違った結果より、何も出ないほうがまし
- **0件と「見ていない」を分ける。** 同じ顔で出すと、無いことの確認にならない
- 判定・集計・変換は純関数として `src/lib/` に置く（テストから直接呼べる形に）

## コミットと push

1つの意味ある変更＝1コミット。本文には**何が起きていたか**と
**なぜその選択にしたか**を書く。半年後に読むのは他人。

末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。

**push は外向きの操作なので、指示があるまでしない。**
`ss-and` への push には `gh auth switch --user ss-and` が要る。

## 製品体制

`.claude/agents/` に **5人の責任者と6人の専門職**を定義してある。

| 責任者 | 持つ責任 | 部下 | 権限 |
|---|---|---|---|
| `pdm` | 何を作るか・**何を作らないか** | `research` | 読むだけ |
| `pm` | 出す／出さないの判定 | `gate` | 読むだけ |
| `pmm` | 製品の言葉すべて | `claims` | 読むだけ |
| `se` | 実装・設計・コード品質 | `builder` `breaker` | **書き込み可** |
| `cs` | 使う人が詰まらないこと | `firstrun` | 読むだけ |

### 運用の掟（これが無いと回らない）

1. **人が呼ぶのは責任者だけ。** 専門職は責任者が呼ぶ
2. **書けるのは `builder` だけ**（`se` の下）。並列させるときは必ず
   別々のファイル群にする。重なるなら `isolation: "worktree"`。
   重なったまま並列にすると**片方の変更が黙って消える**
3. **同時に走らせるのは最大3件。** それ以上は問いが絞れていない証拠
4. **要約の要約を根拠にしない。** 責任者は専門職の生データ（file:line）を受け取り、
   **自分で開いて確かめてから**判断する
5. **責任者は「意見」ではなく「判断」を出す。** 言い切って、外れる条件も書く
6. **4つの門は責任者が通す。** 部下の「通りました」で終わらせない

### 全員に共通の3つ

① 件数を水増ししない（0件なら0件）
② 確信が持てないものは「未確認」と明記
③ 推測ではなく実際にコードを読む

**エージェントの報告はそのまま信じない。** 1件ずつ自分で確かめてから直す。
実際、監査で上がった7件は全件が本物だったが、それは**1件ずつ開いて
確かめたから分かった**こと。確かめずに直すと、無い問題を直すことになる。

## 坂井にしか決められないこと

値付け / 法務 / 外部との約束 / 公開・push・課金開始 / 採用・資金。
たたき台までは作ってよいが、決定は必ず人に返す。

## 公開前

`docs/OPERATIONS.md` の §7 が正本。特に:
- `src/lib/legal.ts` の必須項目（`missingCommerceEntries()` で分かる）
- `vercel.json` の cron は **Vercel Hobby ではデプロイが失敗する**（1日1回まで）
- `ANTHROPIC_API_KEY` を入れるなら、Console で月次上限を設定すること
  （上限が無いと1ワークスペースで月5万円まで理論上到達しうる）
