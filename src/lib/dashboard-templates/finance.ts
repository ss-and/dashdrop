/**
 * Category: finance — dashboard templates.
 * Populated to match the shape in ./support.ts (the reference implementation).
 */
import type { DashboardTemplate } from "../widgets";

export const financeTemplates: DashboardTemplate[] = [];
