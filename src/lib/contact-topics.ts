/**
 * お問い合わせの用件。
 *
 * 画面（クライアント）と受け口（サーバー）の両方が使うので、別の場所に置く。
 * ルート側に置いて画面から import すると、ルートが引き込んでいる
 * サーバー専用の依存（メール送信・DB）がブラウザ側の束に入ってしまう。
 *
 * 自由記述にしないのは、分類できないと返信の優先度が付けられないため。
 * 「不具合の報告」と「機能のご要望」は、読む順番が違う。
 */
export const CONTACT_TOPICS = [
  "導入の相談",
  "料金・お支払い",
  "不具合の報告",
  "機能のご要望",
  "その他",
] as const;

export type ContactTopic = (typeof CONTACT_TOPICS)[number];
